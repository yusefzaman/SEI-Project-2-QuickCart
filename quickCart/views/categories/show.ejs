<%- include('../partials/header') %>

<body>
  <% items.forEach(item => { %>
  <div class="items-box">
    <a href="/categories/items/show/<%= item._id%>"><%= item.itemName %></a>
    <img src="<%= item.itemImg %>" alt="<%= item.itemName %>" />
    <%= item.itemPrice %>
    <% }); %>
  </div>
</body>

</html>